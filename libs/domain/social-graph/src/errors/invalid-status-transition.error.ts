// Re-export InvalidStatusTransitionError from connection-status value object
// where it is defined alongside the transition logic it validates.
export { InvalidStatusTransitionError } from '../value-objects/connection-status';
